{
  "name": "bidder-portal-be",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node --env-file=.env.local --watch server.js",
    "start": "node server.js",
    "test": "node --test tests/portal-api.test.mjs"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}
